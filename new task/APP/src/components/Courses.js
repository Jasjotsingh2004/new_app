// Courses.js
import React from 'react';
import { Link } from 'react-router-dom';

const Courses = () => {
    return (
        <div>
            <h1>Courses Page</h1>
            <Link to="/">Back</Link>
        </div>
    );
};

export default Courses;
